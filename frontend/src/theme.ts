/** Mantine theme: maps to coin-log's existing color scheme and spacing. */
import { createTheme } from "@mantine/core";

export const theme = createTheme({
  defaultRadius: 4,
  fontFamily: "system-ui, sans-serif",
  primaryColor: "blue",
});
